% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal-weights.R
\name{causal-weights}
\alias{causal-weights}
\alias{is_causal_wt}
\alias{estimand}
\alias{estimand<-}
\title{Causal weight accessors}
\usage{
is_causal_wt(x, ...)

estimand(x, ...)

estimand(x, ...) <- value
}
\arguments{
\item{x}{An object to inspect or modify. These generics dispatch on this
argument.}

\item{...}{Arguments passed to methods.}

\item{value}{The causal estimand to assign.}
}
\value{
\code{is_causal_wt()} returns a single logical value. \code{estimand()} returns the
estimand, typically a character string, or \code{NULL} when none is recorded.
\verb{estimand<-()} returns \code{x} with the estimand updated.
}
\description{
Generics for inspecting and setting the metadata carried by causal weight
vectors.
\itemize{
\item \code{is_causal_wt()} tests whether an object is a causal weight vector.
\item \code{estimand()} returns the causal estimand the weights target, such as
\code{"ate"} or \code{"att"}.
\item \verb{estimand<-()} sets the causal estimand.
}
}
\details{
This package defines the generics and the abstract weight class they are
written against. It supplies a method for each generic on \code{causal_wts}, so a
concrete class built with \code{\link[=new_causal_wts]{new_causal_wts()}} inherits all three. The concrete
classes themselves live in the packages that own them, such as \code{propensity},
as do methods for any object that is not a \code{causal_wts} vector.
\code{is_causal_wt()} defaults to \code{FALSE} so that any object that is not a
recognized causal weight vector reports as much; \code{estimand()} and
\verb{estimand<-()} have no meaningful default and signal an error when no method
is registered for the object.
}
\examples{
wts <- new_causal_wts(
  c(1.2, 0.8, 1.5),
  subclass = "cg_toy_wts",
  estimand = "ate"
)

is_causal_wt(wts)
estimand(wts)

# The estimand is metadata on the vector, so it can be replaced in place.
estimand(wts) <- "att"
estimand(wts)

# An object that is not a causal weight vector reports `FALSE` rather than
# erroring.
is_causal_wt(1:3)

# `estimand()` has no meaningful default, so it errors instead.
try(estimand(1:3))

}
\seealso{
\code{\link[=new_causal_wts]{new_causal_wts()}} for the class these generics have methods for,
and the \code{propensity} package for concrete weight classes.
}
