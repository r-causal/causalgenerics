% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipw-pool-methods.R
\name{ipw-pooled-methods}
\alias{ipw-pooled-methods}
\alias{print.ipw_pooled}
\alias{coef.ipw_pooled}
\alias{vcov.ipw_pooled}
\alias{confint.ipw_pooled}
\alias{nobs.ipw_pooled}
\alias{as.data.frame.ipw_pooled}
\title{Methods for a pooled inverse probability weighted result}
\usage{
\method{print}{ipw_pooled}(x, ...)

\method{coef}{ipw_pooled}(object, ...)

\method{vcov}{ipw_pooled}(object, ...)

\method{confint}{ipw_pooled}(object, parm, level = 0.95, ...)

\method{nobs}{ipw_pooled}(object, ...)

\method{as.data.frame}{ipw_pooled}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  conf.int = FALSE,
  conf.level = NULL,
  exponentiate = FALSE
)
}
\arguments{
\item{x}{An \code{ipw_pooled} object.}

\item{...}{Further arguments. These methods ignore them.}

\item{object}{An \code{ipw_pooled} object.}

\item{parm}{The rows to report an interval for, given either as the effect
labels or as their positions. Missing means all of them.}

\item{level}{The confidence level. At the level a row stores, that row's own
limits are returned; at any other level they are rebuilt from t on the
row's degrees of freedom.}

\item{row.names}{A character vector of row names for the returned table, or
\code{NULL} for the automatic ones.}

\item{optional}{Accepted for the \code{\link[base:as.data.frame]{base::as.data.frame()}} generic. Every
column of the table is named, so there is nothing for it to make optional.}

\item{conf.int}{If \code{TRUE}, append \code{conf.low} and \code{conf.high} columns after
the rest of the table. Default is \code{FALSE}.}

\item{conf.level}{The confidence level the bounds report. \code{NULL}, the
default, uses the level the result records.}

\item{exponentiate}{If \code{TRUE}, move the estimates that are on a log scale to
their natural scale, as the section above describes. Default is \code{FALSE}.}
}
\value{
\code{print()} returns its input invisibly.

\code{coef()} returns a named numeric vector of pooled estimates, named by effect
label in the marginal reading and by coefficient name in the conditional one.

\code{vcov()} returns the pooled covariance of those estimates, with the same
labels as dimnames on both margins. A result whose analyses did not all carry
a covariance records none, and raises an error of class
\code{causalgenerics_no_vcov_ipw_pooled}, and of the general class
\code{causalgenerics_no_vcov}, rather than returning one built from the standard
errors, which would report the effects as uncorrelated.

\code{confint()} returns a matrix with one row per effect \code{parm} selects, in the
order \code{parm} gives them, and two columns holding the lower and upper limit,
named by effect label and by the two tail probabilities as percentages. A
character \code{parm} naming an effect the result does not report raises an error
of class \code{causalgenerics_invalid_argument}.

\code{nobs()} returns a single integer.

\code{as.data.frame()} returns a plain data frame with the columns \code{term}, then
\code{contrast} when the result names contrasts, then \code{estimate}, \code{std.error},
\code{statistic}, \code{df}, and \code{p.value}, with \code{conf.low} and \code{conf.high} appended
when they are asked for. The pooled covariance travels on it under the
\code{ipw_vcov} attribute unless the table was exponentiated.
}
\description{
The methods the class \code{\link[=pool_ipw]{pool_ipw()}} returns carries.
\itemize{
\item \code{print()} summarizes the pooling and tabulates the pooled effects.
\item \code{coef()} returns the pooled estimates.
\item \code{vcov()} returns their pooled covariance.
\item \code{confint()} returns their confidence limits.
\item \code{nobs()} returns the smallest number of observations any pooled analysis
was estimated from.
\item \code{as.data.frame()} reports the pooled effects as a tidier-shaped table.
}
}
\details{
These live here for the reason the \code{ipw} methods do. Two packages each
registering \code{print.ipw_pooled()} would collide in the shared S3 method table,
and a caller writing against a pooled result would then get whichever package
was installed last rather than the contract.

Everything they read is a field \code{\link[=pool_ipw]{pool_ipw()}} wrote. They recompute no
pooling, so a result read back out of a saved file answers exactly as one
just built does.

\code{ipw_pooled} deliberately does not inherit from \code{ipw}, and these methods are
deliberately separate from the ones registered against that class. The two
share column names and differ in what the numbers under them mean: a pooled
result reports several analyses rather than one fit and refers its inference
to t rather than to z, so a pooled result reaching \code{confint.ipw()} would come
back with normal limits and nothing would say so.
}
\section{The degrees of freedom}{
Every pooled effect carries its own degrees of freedom, in the \code{df} column of
the \code{estimates} frame. They are the Barnard-Rubin adjusted count, which
depends on how much of that effect's variance came from between the
imputations, so two effects pooled from the same analyses ordinarily differ
in them. \code{confint()} and \code{as.data.frame()} refer each row to t on its own
count rather than to the normal or to a single count for the table; with few
imputations those counts can be in single figures, where the difference is
large.

There is deliberately no \code{df.residual()} method. Residual degrees of freedom
are a property of one fit, and a pooled result has as many as it had
imputations. The per-effect count above is not a residual count and differs
from row to row, so it is reported in the column beside the interval and the
p-value it was used for, and \code{df.residual()} on a pooled result finds no
method and no field and gives \code{NULL}.
}

\section{The confidence limits}{
\code{confint()} returns the limits the result stores for any row reported at the
level asked for, and rebuilds the rest. That is the rule \code{confint()} on an
unpooled result keeps, row by row: a stored pair need not be the one
recomputing gives, since it may have been rounded on its way into the frame.

\code{as.data.frame()} applies the same rule to the frame as a whole, which is
what \code{as.data.frame()} on an unpooled result does. The stored pair comes back
only when every row records the level asked for, since a table mixing stored
limits with rebuilt ones would report two intervals under one pair of column
headings. \code{conf.level = NULL}, its default, names the level the frame records,
which is the level \code{\link[=pool_ipw]{pool_ipw()}} built its limits at; a frame whose rows
disagree records none, and the limits are rebuilt at \code{0.95}.
}

\section{Exponentiating}{
On a marginal table \code{exponentiate = TRUE} means what it means for an unpooled
result. The rows labelled \code{log(rr)} and \code{log(or)} are matched exactly, their
estimate and their bounds move to the natural scale, and the two terms are
relabelled \code{rr} and \code{or}. The interval is settled before the scale is, so a
rebuilt bound is a t half width on the log scale added to an estimate on the
log scale and exponentiated afterwards. The standard error, the statistic,
and the p-value describe the log scale and stay there, and the \code{ipw_vcov}
attribute is dropped rather than carried, since it would describe neither the
table it sits on nor anything else.

A conditional table has no rows labelled as ratios to pick out, so the link
the outcome models were fitted with settles the question for the whole table:
a \code{logit} link puts every coefficient on the log odds scale and a \code{log} link
puts every coefficient on the log risk scale, and both are scales an
exponential undoes. Every estimate moves and no term is relabelled, since the
terms are coefficient names and a coefficient does not change its name with
the scale its estimate is reported on. Every other link raises an error of
class \code{causalgenerics_exponentiate_link}, and of the classes
\code{causalgenerics_invalid_argument_exponentiate} and
\code{causalgenerics_invalid_argument}, rather than exponentiating coefficients
that describe nothing once exponentiated.
}

\examples{
dat <- data.frame(
  x = rep(c(-1.5, -0.5, 0.5, 1.5), each = 5),
  z = rep(c(0, 1), 10),
  y = rep(c(0, 1, 1, 0, 1), 4)
)

imputed <- lapply(1:3, function(i) {
  completed <- dat
  completed$y[i] <- 1 - completed$y[i]
  completed
})

estimate <- list(c(0.20, 0.55), c(0.30, 0.60), c(0.40, 0.65))
std_err <- list(c(0.10, 0.25), c(0.20, 0.30), c(0.30, 0.35))

fits <- Map(
  function(completed, estimate, std.err) {
    new_ipw(
      estimand = "ate",
      wt_mod = glm(z ~ x, family = binomial(), data = completed),
      outcome_mod = glm(y ~ z, family = quasibinomial(), data = completed),
      estimates = data.frame(
        effect = c("rd", "log(rr)"),
        estimate = estimate,
        std.err = std.err,
        z = estimate / std.err,
        ci.lower = estimate - 1.96 * std.err,
        ci.upper = estimate + 1.96 * std.err,
        conf.level = 0.95,
        p.value = 2 * pnorm(-abs(estimate / std.err))
      ),
      se_method = "linearization",
      fit = NULL
    )
  },
  imputed,
  estimate,
  std_err
)

pooled <- pool_ipw(fits)

pooled

coef(pooled)

# Wider than the normal limits would be, since the degrees of freedom are
# what few imputations leave.
confint(pooled)

nobs(pooled)

# The tidier-shaped table, with the degrees of freedom the statistic beside
# it is referred to.
as.data.frame(pooled)

# With an interval, and the ratio on its natural scale.
as.data.frame(pooled, conf.int = TRUE, exponentiate = TRUE)

# Residual degrees of freedom belong to one fit, so a pooled result has no
# method for them; the per-effect count is in the table above.
df.residual(pooled)

}
\seealso{
\code{\link[=pool_ipw]{pool_ipw()}}, which produces these results, and \code{\link[=new_ipw]{new_ipw()}} for the
unpooled result they are pooled from.
}
