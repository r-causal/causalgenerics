% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal-weights-class.R
\name{new_causal_wts}
\alias{new_causal_wts}
\title{Construct a causal weight vector}
\usage{
new_causal_wts(x = double(), subclass, ...)
}
\arguments{
\item{x}{A double vector of weights.}

\item{subclass}{A single non-empty string naming the concrete weight class.}

\item{...}{Metadata to store as attributes on the result. A \code{NULL} value
leaves the attribute absent.}
}
\value{
A vector of class \code{c(subclass, "causal_wts", "vctrs_vctr", "double")}.
}
\description{
\code{new_causal_wts()} is the low-level constructor for the abstract weight class
that the r-causal ecosystem shares. It is intended for package developers
building a concrete weight class, not for end users, and it assumes \code{x} is
already a double vector.
}
\details{
Weights built here are vctrs vectors over a double, so the class vector is
\code{c(subclass, "causal_wts", "vctrs_vctr", "double")}. Callers may rely on that
shape and on its order: \code{subclass} first, so that methods for the concrete
class take precedence, then \code{causal_wts}, so that the concrete class inherits
every method registered against the abstract one. \code{subclass} must therefore be
a single non-empty string. Anything else would change the length of the class
vector or name a class that no method can be registered against.

The dots carry whatever metadata the concrete class records, such as the
estimand or the exposure groups. This constructor neither names nor types
those fields. A field passed as \code{NULL} leaves its attribute absent rather than
recording a \code{NULL}, which is what lets a concrete constructor default its
metadata to \code{NULL} and pass it straight through.

The abstract layer stops at construction and the metadata accessors. A
concrete class still owns its own \code{vec_ptype2()}, \code{vec_cast()},
\code{vec_ptype_abbr()}, \code{vec_restore()}, and \code{vec_arith()} methods: vctrs resolves
the first three on \code{class(x)[[1]]} alone and so never finds a \code{causal_wts}
method, and the last two have to reconcile metadata that this package knows
nothing about.
}
\examples{
wts <- new_causal_wts(c(1.2, 0.8), subclass = "my_wts", estimand = "ate")
class(wts)
estimand(wts)
}
\seealso{
\code{\link[=estimand]{estimand()}} and \code{\link[=is_causal_wt]{is_causal_wt()}}, the accessors this class supplies
methods for.
}
