# the subclass error states the contract

    Code
      new_causal_wts(1, subclass = c("a", "b"))
    Condition
      Error in `new_causal_wts()`:
      ! `subclass` must be a single non-empty string.

