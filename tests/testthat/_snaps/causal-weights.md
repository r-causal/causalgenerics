# estimand() and estimand<-() error for an unregistered class

    Code
      estimand(x)
    Condition
      Error in `estimand.default()`:
      ! No `estimand()` method for an object of class <cg_unregistered>.

