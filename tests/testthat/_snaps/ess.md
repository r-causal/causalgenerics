# the non-numeric error states the contract

    Code
      ess(c("a", "b"))
    Condition
      Error in `ess.default()`:
      ! `x` must be a numeric vector.

---

    Code
      ess(NULL)
    Condition
      Error in `ess.default()`:
      ! `x` must be a numeric vector.

